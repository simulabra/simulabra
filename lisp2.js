/*
 * probably let's just burn it all
 * start afresh with syntax
 *
 * name ~class-name !type-name %arg-name ^return :symbol .(msg-name x y) $(macro-name :s .(frob))
 * %arg(msg .) ~debug(log "testing") .(x | + 5 | sqrt)
 * [2 4 6 8]
 * { name :something value %x(* 42 | + 5) }
 * &%conn(async-method)
 *
 * traits?
 * desires for the shape of something?
 *
 * ObjectLiteral
 * Literal
 */
