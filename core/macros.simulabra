$(macro poso [name $(rest vars)]
  $(def %name ~class(new {
    slots %vars(map $(do ~var(new { name %it })) | intoObject)
  }))
)

$(defn test []
  $(let c $(poso a b))
  $(let ci %c(new { a 2 b 3 }))
  ~debug(log %ci)
)
