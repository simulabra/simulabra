$(macro poso [name @vars]
  `~class(new {
    name ,%name
    slots ,%vars(objectify $(do [%name `~var(new { name ,%it })]))
  })
)

$(defn test []
  $(poso c a b)
  $(let ci ~c(new { a 2 b 3 }))
  ~debug(log %ci)
)
