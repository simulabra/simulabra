$(macro poso [name $(rest vars)]
  $(def $(qq ~class(new {
    name ,%name
    slots ,%vars(map $(do $(qq ~var(new { name ,%it }))) | intoObject)
  })))
)

$(defn test []
  $(let c $(poso a b))
  $(let ci %c(new { a 2 b 3 }))
  ~debug(log %ci)
)
