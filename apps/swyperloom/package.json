{
  "name": "swyperloom",
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "simulabra": "file:../.."
  },
  "scripts": {
    "build": "bun build src/app.js --outdir dist",
    "dev": "bun build src/app.js --outdir dist --watch",
    "test": "cd ../.. && bun run src/runner.js apps/swyperloom/tests/"
  }
}
